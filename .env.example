# Copy to `.env` and fill in. `.env` is gitignored — never commit real secrets.
# Runtime is OpenAI-only (hackathon credits); Anthropic APIs never enter the product.
OPENAI_API_KEY=
SUPABASE_URL=
SUPABASE_SECRET_KEY=
