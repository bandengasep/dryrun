# DryRun — product spec

> **Placeholder — not yet transcribed.** The full product spec
> (`interview-compiler-spec.md`) is not in the repo yet. Paste its contents here:
> `CLAUDE.md` and the README both reference `docs/spec.md` as the canonical spec, and
> the three 12-Jul decision-log entries are sourced from it.

For the operational summary (build order, cut list, locked stack, ground rules), see
`CLAUDE.md` at the repo root.
