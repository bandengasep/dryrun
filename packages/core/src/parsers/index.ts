// JD & resume parsers. Commit 1: OpenAI Responses API strict structured output via
// `openai.responses.parse({ text: { format: zodTextFormat(RequirementLine, "jd") } })`,
// every parsed line carrying its SourceSpan. Typed stub for now so the module compiles.
export {};
