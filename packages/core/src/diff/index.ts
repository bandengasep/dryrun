// Gap diff engine. Commit 2: pgvector embedding match (JD requirement ↔ nearest resume
// lines) + LLM adjudication → typed set-difference over GapKind, every row carrying
// jdSpan + resumeSpan receipts. Typed stub for now so the module compiles.
export {};
